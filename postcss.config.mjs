/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
   // tailwindcss: {},
   'postcss-import': {},
   'tailwindcss': {},
   'autoprefixer': {},

  },
};

export default config;
